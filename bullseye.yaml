# bullseye.yaml — convergence targets for this project.
#
# Targets are desired project states expressed as testable properties,
# with dependency tracking and frontier computation. This file is the
# source of truth: read and written by the bullseye MCP server.
#
# AGENT RULE: Do NOT edit this file directly unless the user explicitly
# instructs you to, or the file is unloadable and you are performing an
# emergency repair. Prefer Bullseye MCP/CLI mutation tools (open/query/
# commit). Direct edits are detected via content_hash below.
#
# What is bullseye / how do I install it?
#   https://github.com/marcelocantos/bullseye
#
# This banner is regenerated by bullseye on every write — edit targets
# only through tools, not these lines.
# content_hash: sha256:f32b80d6db5a24907473357811e530bde19db1b0770a2043bcee17ab3ee715ea

schema_version: 5
targets:
  T1:
    name: 'Phase 0: Mac daemon exposes vector scene create/read over MCP'
    status: identified
    value: 50.0
    cost: 13.0
    acceptance:
    - orthograph daemon serves HTTP MCP and stays up
    - orthograph_status returns structured status
    - orthograph_open creates a document persisted under ~/.orthograph/
    - orthograph_create places at least rect objects; orthograph_scene returns them with stable ids and bbox
    - orthograph_render returns a PNG for the active document (even if minimal)
    - make bullseye green; agents-guide documents the tools
    context: 'Scaffold landed 2026-08-05 (CLI skeleton, doc types, design). Phase 0 is the vertical slice before iPad/pigeon: agent can create and re-read a vector diagram on the Mac alone.'
    tags:
    - phase-0
    - mcp
    - core
    origin: bullseye_init
    discovered: 2026-08-05
